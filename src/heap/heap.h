#ifndef HEAP_H
#define HEAP_H

#include "max_heap.h"
#include "min_heap.h"

namespace heap {
    const Nat Izq(Nat n);

    const Nat Der(Nat n);

    const Nat Padre(Nat n);

    const Nat potenciaDeDos(Nat n);
}

#endif //HEAP_H
